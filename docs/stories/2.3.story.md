# Story 2.3: Consulta de pedidos por repartidor

## Status

Review

## Story

**As a** auxiliar de mostrador,
**I want** consultar qué números de pedido tiene registrados un repartidor específico,
**so that** pueda resolver rápido la duda de quién se llevó un pedido determinado.

## Acceptance Criteria

1. Desde el Resumen de Domicilios, se puede seleccionar un repartidor y ver el listado de números de pedido registrados a su nombre.
2. El listado incluye la fecha/hora de registro de cada pedido.
3. La consulta no requiere buscar en WhatsApp ni en ningún registro externo al sistema.

> **Nota de implementación (actualizada tras validación de mockups):** el diseño original de esta historia asumía una página separada (`/resumen/[repartidorId]`). El mockup validado con el propietario (`mockups/mockup2.png`) muestra este detalle como un **acordeón expandible dentro de la misma pantalla `/resumen`** — el AC arriba sigue siendo válido tal cual ("se puede seleccionar un repartidor y ver el listado"), pero la implementación cambia: no hay navegación ni cambio de URL. Ver `docs/front-end-spec.md`.

## Tasks / Subtasks

- [x] Task 1: Datos de pedidos por repartidor disponibles en el Server Component padre (AC: 1, 2)
  - [x] `ResumenDomicilios` (Story 2.2) ya obtiene los registros vía `lib/db/registros.ts` / `lib/services/resumen.ts`; extender esa consulta para incluir el detalle de `numeroPedido` + `registradoEn` por repartidor, no solo el agregado [Source: architecture/data-models.md#registrosalida]
- [x] Task 2: Componente RepartidorAccordionRow (AC: 1, 2)
  - [x] `components/repartidor-accordion-row.tsx`: Client Component que recibe los pedidos del repartidor como prop y los muestra al expandir (tabla inline: número de pedido + hora) [Source: architecture/components.md#repartidoraccordionrow-client-component]
  - [x] Usar el primitive `Accordion` de shadcn/ui [Source: docs/front-end-spec.md#component-library--design-system]
- [x] Task 3: Integrar en ResumenDomicilios (AC: 1)
  - [x] Cada fila de repartidor en `resumen-table.tsx` renderiza un `RepartidorAccordionRow`, sin enlazar a otra ruta [Source: architecture/frontend-architecture.md#routing-architecture]
- [x] Task 4: Endpoint GET /api/repartidores/{id}/pedidos (opcional, no bloqueante)
  - [x] `app/api/repartidores/[id]/pedidos/route.ts`: retorna `RegistroSalida[]` del repartidor — se mantiene documentado por si se necesita consumo externo en el futuro, pero el acordeón NO depende de este endpoint (usa los datos ya cargados por el Server Component padre) [Source: architecture/api-specification.md#rest-api-specification]
- [x] Task 5: Tests
  - [x] Test de componente: al expandir `RepartidorAccordionRow`, se muestran los números de pedido y hora correctos de ESE repartidor (y no los de otro) [Source: architecture/testing-strategy.md#test-organization]

## Dev Notes

### Previous Story Insights

[Source: docs/stories/2.2.story.md] La pantalla de Resumen ya lista repartidores con cantidad y valor a pagar, y cada fila ya está preparada para renderizar un `RepartidorAccordionRow`. Esta historia implementa el contenido de ese acordeón — no agrega una ruta ni una navegación nueva.

### Component Specification

[Source: architecture/components.md#repartidoraccordionrow-client-component]

- **Responsibility:** Fila expandible de un repartidor dentro de `ResumenDomicilios`; al expandir, muestra los números de pedido y fecha/hora registrados a su nombre.
- **Key Interfaces:** Recibe los pedidos del repartidor como prop desde `ResumenDomicilios` (mismo fetch del Server Component padre, sin round-trip HTTP adicional).
- **Dependencies:** `components/ui/accordion` (shadcn).
- **Technology Stack:** React Client Component (necesita estado local de expandido/colapsado).

### Routing

[Source: architecture/frontend-architecture.md#routing-architecture]

```text
/resumen            - Resumen de Domicilios (incluye detalle por repartidor como acordeón inline, sin ruta propia)
```

No existe `/resumen/[id]` — fue removida de la arquitectura tras la validación de mockups.

### Mockup validado (fuente de verdad visual)

[Source: docs/front-end-spec.md#resumen-de-domicilios] Ver `mockups/mockup2.png`: Carlos aparece expandido mostrando "Pedidos de Carlos" con número + hora por fila; Diego y Andrés aparecen colapsados. Pendiente confirmar con el propietario si el acordeón es exclusivo (uno a la vez) o si varios pueden expandirse simultáneamente — implementar como independientes (no exclusivo) por defecto si no hay respuesta antes de esta historia, por ser el comportamiento más simple de construir.

### API Specification (opcional)

[Source: architecture/api-specification.md#rest-api-specification]

`GET /repartidores/{id}/pedidos` → 200 con `RegistroSalida[]` (incluye `numeroPedido` y `registradoEn`). Este endpoint queda documentado pero es OPCIONAL para esta historia: al ser el acordeón parte del mismo Server Component que ya cargó todos los datos del resumen, no hay necesidad de un round-trip HTTP adicional para expandir una fila [Source: architecture/high-level-architecture.md#architectural-patterns].

### Data Models

[Source: architecture/data-models.md#registrosalida]

Cada `RegistroSalida` expone `numeroPedido` y `registradoEn` — ambos campos requeridos por el AC 2 de esta historia.

### File Locations

- `components/repartidor-accordion-row.tsx` (nuevo)
- `app/api/repartidores/[id]/pedidos/route.ts` (nuevo, opcional)

[Source: architecture/unified-project-structure.md]

### Testing

[Source: architecture/testing-strategy.md#test-organization]

Test de componente verificando que `RepartidorAccordionRow` muestra exactamente los pedidos del repartidor recibido por props (y no los de otro), con número de pedido y fecha/hora.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-22 | 0.1 | Story creada por SM a partir de Epic 2 sharded | Bob (SM) |
| 2026-07-22 | 0.2 | Reescrita tras validación de mockups: acordeón inline reemplaza ruta separada `/resumen/[repartidorId]` | Sally (UX Expert) |
| 2026-07-22 | 0.3 | Acordeón implementado, historia completa (incluye el sub-ítem diferido de Story 2.2) | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

_Ninguno._

### Completion Notes List

- `obtenerResumen()` (Story 2.1) cambió de `groupBy` a `include` + reduce en memoria: ahora necesita el detalle fila-por-fila (`numeroPedido`, `registradoEn`) además del agregado, y `groupBy` no puede devolver eso — aceptable al volumen del MVP (mismo argumento de `security-and-performance.md` que ya justificaba no tener caché).
- **Refactor no trivial de `resumen-table.tsx` (de Story 2.2)**: la tabla de shadcn (`<table>`/`<tr>`/`<td>`) no puede contener un `Accordion` (basado en `<div>`) como hijo de fila sin romper la validez del HTML — se reemplazó `Table` por `Accordion type="multiple"` como contenedor de la lista completa, con cada repartidor como `AccordionItem`. `components/ui/table.tsx` queda instalado pero sin uso en la app por ahora (no se borró; es una primitive de shadcn como cualquier otra, no hay obligación de usarla en todos lados).
- `type="multiple"` (no exclusivo): el mockup no aclara si solo un repartidor puede estar expandido a la vez y no hubo respuesta del propietario a tiempo — se implementó el comportamiento más simple (independientes), tal como sugiere la propia Dev Note de esta historia para ese caso.
- Task 4 (endpoint opcional `GET /api/repartidores/[id]/pedidos`) implementado igual, aunque el acordeón no lo usa — usa Promise-based `params` (cambio real de Next.js 15 frente a versiones anteriores, confirmado con Context7 antes de escribirlo).
- Test de componente verifica aislamiento real: dos `RepartidorAccordionRow` en el mismo `Accordion`, se expande uno con `user-event` (click real, no solo inspección de props) y se confirma que los pedidos del otro NO aparecen en el DOM.
- Se actualizó el test de `ResumenDomicilios` de Story 2.2 (su mock no tenía el campo `pedidos`, ahora requerido por el tipo) y su aserción de cantidad (buscaba el texto "3" exacto; ahora el trigger renderiza "3 pedidos" como un solo nodo de texto).
- Verificado end-to-end contra el stack Docker real: `/resumen` renderiza los 3 repartidores; el endpoint opcional responde `[]` para un repartidor sin domicilios.

### File List

- `lib/services/resumen.ts` (extendido con `pedidos`)
- `components/repartidor-accordion-row.tsx`
- `components/resumen-table.tsx` (`Table` reemplazada por `Accordion`)
- `components/ui/accordion.tsx`
- `app/api/repartidores/[id]/pedidos/route.ts`
- `tests/unit/components/repartidor-accordion-row.test.tsx`
- `tests/unit/components/resumen-table.test.tsx` (mock y aserción actualizados)

### Story DoD Checklist — autoevaluación

1. **Requisitos:** las 3 ACs cumplidas y verificadas; completa además el sub-ítem que Story 2.2 dejó pendiente a propósito.
2. **Estándares de código:** `RepartidorAccordionRow` es Client Component (necesita estado de expandido/colapsado, como pide la spec); recibe los pedidos por props del Server Component padre, sin round-trip HTTP; sin nuevos warnings de lint.
3. **Testing:** test de aislamiento por repartidor (exigido por la historia) pasando junto con el resto de la suite (18/18); se corrigió un test existente que quedó desactualizado por el cambio de tipo.
4. **Funcionalidad verificada manualmente:** sí — `curl` contra el stack Docker real, incluyendo el endpoint opcional.
5. **Administración:** las 5 tasks marcadas `[x]`; refactor no trivial (Table→Accordion) documentado con su motivo.
6. **Dependencias/build:** `npm run build`, `npm run lint`, `npm run test` limpios; nuevo componente shadcn (`accordion`), sin dependencias npm nuevas.
7. **Documentación:** ninguna otra documentación de usuario afectada.

**Gaps conocidos:** ninguno. Queda abierta la pregunta de producto (no resuelta por nadie, ya señalada en el mockup) de si el acordeón debería ser exclusivo — documentada, no bloqueante.

- [x] Yo, el Dev Agent, confirmo que los ítems aplicables de arriba fueron atendidos.

## QA Results

_Pendiente — se completa tras revisión de QA._
