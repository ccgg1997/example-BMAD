# Story 1.3: Modelo de datos de repartidores y pedidos

## Status

Review

## Story

**As a** desarrollador del sistema,
**I want** un modelo de datos que represente repartidores y registros de salida de pedidos,
**so that** el sistema pueda guardar y consultar de forma confiable quién lleva cada pedido.

## Acceptance Criteria

1. Existe una entidad Repartidor con al menos nombre e identificador único.
2. Existe una entidad Pedido/Registro de Salida con repartidor asignado, número de pedido, y fecha/hora de registro.
3. El número de pedido no se reinicia por día (ver FR8); el modelo garantiza unicidad del número de pedido a nivel de todo el sistema o de un rango temporal definido con el propietario.
4. La tarifa por domicilio es un valor configurable (ver FR7), no un valor fijo en código.
5. Existe un script de seed que crea los repartidores iniciales conocidos hoy (Carlos, Diego, Andrés) y la fila inicial de tarifa ($1.000 COP), de forma que la Story 1.4 tenga datos reales con los que probar.

## Tasks / Subtasks

- [x] Task 1: Definir schema Prisma (AC: 1, 2, 3, 4)
  - [x] Modelo `Repartidor`: `id`, `nombre` (unique), `activo` [Source: architecture/data-models.md#repartidor]
  - [x] Modelo `RegistroSalida`: `id`, `numeroPedido` (unique global), `repartidorId` (FK), `registradoEn`, `tarifaAplicada` [Source: architecture/data-models.md#registrosalida]
  - [x] Modelo `ConfiguracionSistema` (singleton): `id` fijo, `tarifaDomicilio` [Source: architecture/data-models.md#configuracionsistema]
  - [x] Índices: `numero_pedido` (unique), `repartidor_id`, `registrado_en` [Source: architecture/database-schema.md]
- [x] Task 2: Generar y aplicar migración inicial (AC: 1, 2, 3, 4)
  - [x] `npx prisma migrate dev` creando las 3 tablas [Source: architecture/database-schema.md]
- [x] Task 3: Script de seed (AC: 5)
  - [x] Crear `prisma/seed.ts`: inserta repartidores Carlos, Diego, Andrés (activos) y fila `configuracion_sistema` con `tarifa_domicilio = 1000` [Source: docs/prd/epic-1-fundamento-autenticacion-y-registro-de-salida.md#story-13-modelo-de-datos-de-repartidores-y-pedidos]
  - [x] Configurar `prisma.seed` en `package.json` para que corra con `npx prisma db seed`
- [x] Task 4: Exponer capa de acceso a datos base (para uso en historias siguientes)
  - [x] `lib/db/client.ts`: instancia única de PrismaClient [Source: architecture/unified-project-structure.md]
- [x] Task 5: Tests
  - [x] Test de integración: seed puebla exactamente 3 repartidores activos y 1 fila de configuración con tarifa 1000 [Source: architecture/testing-strategy.md#test-organization]
  - [x] Test: insertar dos `RegistroSalida` con el mismo `numeroPedido` falla por la restricción UNIQUE [Source: architecture/database-schema.md]

## Dev Notes

### Previous Story Insights

[Source: docs/stories/1.1.story.md] Prisma ya fue instalado e inicializado (schema vacío) en Story 1.1, junto con Docker Compose para PostgreSQL. Esta historia define el schema real y aplica la primera migración funcional.

### Data Models

[Source: architecture/data-models.md]

**Repartidor**
```typescript
interface Repartidor {
  id: string;
  nombre: string;
  activo: boolean;
}
```
Relaciones: un Repartidor tiene muchos RegistroSalida (1—N).

**RegistroSalida**
```typescript
interface RegistroSalida {
  id: string;
  numeroPedido: number;
  repartidorId: string;
  registradoEn: string;
  tarifaAplicada: number;
}
```
`numeroPedido` es único globalmente — implementa a la vez FR4 (no doble asignación) y FR8 (no se repite entre días). `tarifaAplicada` es un snapshot al momento del registro, nunca se recalcula retroactivamente [Source: architecture/coding-standards.md#critical-fullstack-rules].

**ConfiguracionSistema** (singleton)
```typescript
interface ConfiguracionSistema {
  id: string;
  tarifaDomicilio: number;
}
```
Leída por el flujo de Registro de Salida (Story 1.4) para fijar `tarifaAplicada` en cada nuevo registro.

### Database Schema

[Source: architecture/database-schema.md]

```sql
CREATE TABLE repartidor (
    id          TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
    nombre      TEXT NOT NULL UNIQUE,
    activo      BOOLEAN NOT NULL DEFAULT true
);

CREATE TABLE configuracion_sistema (
    id                TEXT PRIMARY KEY DEFAULT 'default',
    tarifa_domicilio  NUMERIC(10,2) NOT NULL DEFAULT 1000
);

CREATE TABLE registro_salida (
    id               TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
    numero_pedido    INTEGER NOT NULL UNIQUE,
    repartidor_id    TEXT NOT NULL REFERENCES repartidor(id),
    registrado_en    TIMESTAMPTZ NOT NULL DEFAULT now(),
    tarifa_aplicada  NUMERIC(10,2) NOT NULL
);

CREATE INDEX idx_registro_salida_repartidor ON registro_salida(repartidor_id);
CREATE INDEX idx_registro_salida_registrado_en ON registro_salida(registrado_en);
```

### File Locations

- `prisma/schema.prisma` (modificar)
- `prisma/migrations/` (nueva migración, generada por Prisma)
- `prisma/seed.ts` (nuevo)
- `lib/db/client.ts` (nuevo)

[Source: architecture/unified-project-structure.md]

### Testing

[Source: architecture/testing-strategy.md]

- Test de integración cubriendo el seed y la restricción de unicidad de `numero_pedido` — esta última es la pieza central que en Story 1.5 se usará para resolver conflictos, así que debe quedar bien probada aquí.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-22 | 0.1 | Story creada por SM a partir de Epic 1 sharded | Bob (SM) |
| 2026-07-22 | 0.2 | Schema, migración, seed, tests — historia completa | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

_Ninguno._

### Completion Notes List

- **`id` generado por Prisma (`@default(uuid())`), no por Postgres**: `architecture/database-schema.md` usa `DEFAULT gen_random_uuid()::text` a nivel de columna; se optó por la generación del lado de Prisma para no depender de si `gen_random_uuid()` está disponible sin la extensión `pgcrypto` según la versión exacta de Postgres — funcionalmente equivalente (id de texto único), solo cambia dónde se genera.
- **`registradoEn` corregido a `TIMESTAMPTZ`**: la primera migración generada por Prisma usó `TIMESTAMP(3)` (sin zona horaria), el tipo por defecto para `DateTime`; `database-schema.md` pide explícitamente `TIMESTAMPTZ`. Se agregó `@db.Timestamptz(3)` y se regeneró la migración (se recreó la DB de desarrollo desde cero para no dejar una migración "init" seguida de una migración "fix" en el historial de un proyecto que todavía no se ha desplegado a ningún lado).
- `lib/db/client.ts` ya existía desde Story 1.1 (Task 4 de esa historia, para el health-check) — no se creó de nuevo, solo se reutiliza.
- Seed usa `upsert` (no `create`), por lo que correr `npx prisma db seed` más de una vez no duplica repartidores ni la fila de configuración.
- `prisma/seed.ts` importa `lib/db/client` con ruta relativa (`../lib/db/client`), no el alias `@/` — el script corre standalone vía `tsx` fuera del contexto de build de Next.js/Vitest, y no hay garantía de que `tsx` resuelva el alias de `tsconfig.json` de la misma forma.
- Tests de integración corren contra la Postgres real de Docker Compose (no una DB de test aislada ni mocks). El test de unicidad usa un `numeroPedido` aleatorio en un rango alto y limpia su propio registro al final (`deleteMany`) para no dejar residuo entre corridas — igual que el test de seed verifica por nombre (`Carlos`/`Diego`/`Andrés`), no por conteo total de la tabla, para no volverse frágil cuando historias futuras inserten más repartidores de prueba.
- Nueva dependencia: `tsx` (exacta, devDependency) para correr `prisma/seed.ts` vía `npx prisma db seed`.

### File List

- `prisma/schema.prisma` (modelos reales)
- `prisma/migrations/20260722213235_init_schema/migration.sql`
- `prisma/seed.ts`
- `package.json`, `package-lock.json` (`tsx` agregado, bloque `prisma.seed`)
- `tests/integration/db/seed.test.ts`
- `tests/integration/db/registro-salida-unique.test.ts`

### Story DoD Checklist — autoevaluación

1. **Requisitos:** las 5 ACs cumplidas y verificadas contra la Postgres real (no solo `npm run test`): 3 tablas creadas, seed con los 3 repartidores + tarifa 1000, restricción UNIQUE de `numero_pedido` confirmada.
2. **Estándares de código:** naming DB en snake_case vía `@map`/`@@map` (interfaces TS quedan camelCase, coherente con `data-models.md`); sin warnings de lint nuevos.
3. **Testing:** 2 archivos de test nuevos, ambos verificando comportamiento real de la base de datos, no solo del ORM en memoria.
4. **Funcionalidad verificada manualmente:** sí — `psql` directo contra el contenedor confirmando filas del seed, además de los tests automatizados.
5. **Administración:** las 5 tasks marcadas `[x]`; 2 decisiones no obvias documentadas (id generado por Prisma vs Postgres, fix de `TIMESTAMPTZ`).
6. **Dependencias/build:** `npm run build`, `npm run lint`, `npm run test` limpios (9/9); `tsx` fijado en versión exacta.
7. **Documentación:** ninguna otra documentación de usuario afectada.

**Gaps conocidos:** ninguno.

- [x] Yo, el Dev Agent, confirmo que los ítems aplicables de arriba fueron atendidos.

## QA Results

_Pendiente — se completa tras revisión de QA._
